import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.bluevibestudios.trademachine',
  appName: 'Basketball Trade Machine',
  webDir: 'out',
  backgroundColor: '#0a0a0b',
  ios: {
    backgroundColor: '#0a0a0b',
    contentInset: 'always',
  },
};

export default config;
